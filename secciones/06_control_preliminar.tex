\section{Diseño preliminar de Sistemas de Control I}

\subsection{Variable controlada (PV)}

La variable controlada es la temperatura del fluido de proceso (agua o agua con detergente diluido) en el tanque de mezcla del sistema CIP portátil, medida mediante un sensor RTD PT100 conectado a un módulo de expansión analógica SM 1231 (escalamiento \texttt{NORM\_X/SCALE\_X} en TIA Portal).

De las variables de proceso disponibles (temperatura, caudal, nivel, presión y conductividad), la temperatura es la única con una dinámica lo suficientemente lenta y bien caracterizable con los recursos del laboratorio para diseñar y validar un controlador P/PI/PID completo dentro del alcance del curso de Sistemas de Control I.

\subsection{Variable manipulada (MV)}

La variable manipulada corresponde a la potencia eléctrica suministrada al elemento calefactor del sistema, constituido por una resistencia eléctrica sumergible de potencia nominal de $1500\,\mathrm{W}$. Esta variable es modificada por el PLC mediante una estrategia de control por proporción de tiempo (\textit{Time Proportioning Control}), implementada mediante el bloque \texttt{PID\_Compact} de TIA Portal \cite{siemens2021pid}. La señal de control es aplicada a la resistencia a través de un relé de estado sólido (SSR) con conmutación en cruce por cero \cite{rashid2014power}.

Para la modulación de la potencia de la resistencia se evaluaron dos estrategias de control para una carga resistiva de corriente alterna (CA): el control por ángulo de fase mediante TRIAC y el control proporcional en el tiempo mediante SSR con conmutación en cruce por cero \cite{mohan2007power}. La selección de la estrategia se realizó considerando principalmente la dinámica térmica del proceso y los requerimientos de implementación del sistema CIP portátil.

La constante de tiempo térmica estimada del proceso es del orden de varias horas, con un valor aproximado de $\tau \approx 261{,}6\,\mathrm{min}$, correspondiente al modelo obtenido en la sección 3. Este valor es considerablemente superior al período de conmutación empleado para la modulación proporcional en el tiempo mediante el SSR, cuyo orden de magnitud es de segundos. En consecuencia, desde el punto de vista de la dinámica térmica, el proceso no responde a las conmutaciones individuales del SSR, sino al valor promedio de la potencia suministrada durante cada período de modulación \cite{ogata2010modern, dorf2021modern}.

Por lo anterior, ambas estrategias pueden producir un efecto térmico equivalente siempre que entreguen el mismo valor promedio de potencia a la resistencia. Sin embargo, el control por ángulo de fase mediante TRIAC presenta como desventaja la generación de componentes armónicas y mayores niveles de interferencia electromagnética (EMI) conducida, además de requerir una etapa de disparo más compleja \cite{rashid2014power, mohan2007power}.

En contraste, la solución basada en SSR con conmutación en cruce por cero y control proporcional en el tiempo permite obtener una regulación adecuada de la potencia promedio sin introducir una modulación de fase de la tensión de alimentación \cite{rashid2014power}. Por esta razón, se seleccionó la estrategia de \textit{Time Proportioning Control} mediante SSR para el sistema, debido a su menor complejidad de implementación, robustez frente a las condiciones de operación y adecuación a la dinámica lenta del proceso térmico.

En términos de control, la variable manipulada puede expresarse como la potencia promedio suministrada a la resistencia, $P_{\mathrm{prom}}$, cuyo valor se encuentra limitado por la potencia nominal del elemento calefactor:

\begin{equation}
0 \leq P_{\mathrm{prom}} \leq 1500\,\mathrm{W}
\end{equation}

Cuando se utiliza control proporcional en el tiempo, la potencia promedio puede aproximarse mediante:

\begin{equation}
P_{\mathrm{prom}} = D P_{\mathrm{nom}}
\end{equation}

donde $D$ corresponde al ciclo de trabajo, definido como la fracción del período de modulación durante la cual el SSR permanece conduciendo, y $P_{\mathrm{nom}} = 1500\,\mathrm{W}$ corresponde a la potencia nominal de la resistencia. Por tanto, para un ciclo de trabajo comprendido entre $0$ y $1$, la potencia promedio puede regularse entre $0$ y $1500\,\mathrm{W}$.

\subsection{Definición del lazo de control}
\input{tablas/tabla_variables_control}

\subsection{Diagrama de bloques}
\insertarfigura{diagrama_bloques_control.pdf}{Diagrama de bloques del sistema térmico.}{fig:bloquescontrol}

\subsection{Modelo dinámico preliminar}
\campo{Presente el balance de energía, parámetros, condiciones iniciales, función de transferencia o modelo equivalente, punto de operación y rango de validez.}

Se modela el tanque de mezcla como un sistema térmico de parámetros concentrados (\textit{lumped capacitance}), es decir, se asume una temperatura uniforme en todo el volumen activo en cada instante. Este supuesto es razonable debido al efecto de mezclado producido por la recirculación del propio sistema CIP y es válido cuando el número de Biot del sistema es suficientemente pequeño ($Bi \ll 1$) [9].

El balance de energía instantáneo, análogo al de un tanque agitado con calentamiento eléctrico [10], se expresa mediante:

\begin{equation}
Q_h(t) - UA\left(T(t)-T_{\mathrm{amb}}\right)
\end{equation}

donde $m$ es la masa activa de fluido, expresada en $\mathrm{kg}$; $c_p$ es el calor específico del agua, con un valor aproximado de $4186,\mathrm{J/(kg,K)}$ [9]; $Q_h(t)$ es la potencia térmica entregada por la resistencia, expresada en $\mathrm{W}$; $UA$ es el coeficiente global de transferencia de calor asociado a las pérdidas hacia el ambiente, expresado en $\mathrm{W/K}$; y $T_{\mathrm{amb}}$ es la temperatura ambiente, considerada como una perturbación del sistema.

Linealizando el modelo alrededor de un punto de operación y aplicando la transformada de Laplace, bajo la condición inicial $T(0)=T_{\mathrm{amb}}$, se obtiene el modelo de primer orden estándar [2], [3]:
\begin{equation}
\frac{\Delta T(s)}{Q_h(s)}
\end{equation}
\begin{equation}
\frac{K}{\tau s+1}
\end{equation}

donde la ganancia estática del proceso y la constante de tiempo están dadas, respectivamente, por:

\begin{equation}
K=\frac{1}{UA}
\end{equation}

\begin{equation}
\tau=\frac{m c_p}{UA}
\end{equation}

En este modelo, $K$ representa la ganancia estática del proceso, expresada en $\mathrm{K/W}$, mientras que $\tau$ representa la constante de tiempo térmica del sistema, expresada en segundos. El modelo permite caracterizar la relación dinámica entre la potencia térmica suministrada por la resistencia y el incremento de temperatura del fluido respecto al punto de operación.

\begin{equation}
\boxed{
G(s)=\frac{0{,}25}{15697{,}5s+1}
}
\end{equation}

\subsection{Simulación en lazo abierto}
\insertarfigura{respuesta_lazo_abierto.pdf}{Respuesta preliminar del modelo en lazo abierto.}{fig:lazoabierto}
\campo{Analice ganancia, constante de tiempo, retardo, estabilidad y limitaciones observadas.}

\subsection{Especificaciones preliminares de desempeño}
\input{tablas/tabla_especificaciones_control}
